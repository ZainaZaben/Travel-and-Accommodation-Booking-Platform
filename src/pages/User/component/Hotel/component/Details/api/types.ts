import { BaseResponse } from "@/types";
export interface Response extends BaseResponse {
  reviewId: number;
  customerName: string;
  rating: number;
  description: string;
}
export interface Response extends BaseResponse {
 name:string,
 description:string
}
